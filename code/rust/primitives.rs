#[allow(unused_variables)]
fn main() {
// BEGIN_CODE
	// unsigned values
	let a: u8;
	let b: u16;
	let c: u32;
	let d: u64;

	// Boolean values
	let e: bool;

	// signed values
	let f: char;
	let g: i8;
	let h: i16;
	let i: i32;
	let j: i64;

	// floating point values
	let k: f32;
	let l: f64;
// END_CODE
}
